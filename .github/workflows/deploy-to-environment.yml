name: Deploy to environment

on:
  pull_request:
    types:
      - opened
      # TODO: Remove me
      - synchronize

jobs:
  setup:
    runs-on: ubuntu-latest

    steps:
      - name: Setup
        uses: actions/setup
