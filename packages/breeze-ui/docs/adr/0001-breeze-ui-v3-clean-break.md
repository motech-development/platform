# Replace Breeze UI with a clean v3 release

Breeze UI will retain the `@motech-development/breeze-ui` package identity and replace its legacy API in a new v3 major release. Existing applications remain pinned to v2 until they are rebuilt, so v3 will not include compatibility exports, migration shims, or legacy implementation constraints.
