/** Stable inclusive date range crossing the Breeze public API. */
export interface DateRangeValue {
  /** Inclusive range end in `YYYY-MM-DD` format. */
  end: string;
  /** Inclusive range start in `YYYY-MM-DD` format. */
  start: string;
}
