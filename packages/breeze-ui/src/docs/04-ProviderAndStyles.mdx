import { Meta } from '@storybook/addon-docs/blocks';

<Meta
  title="Guides/04 Provider and styles"
  summary="Document tokens, scoped painting and automatic colour schemes."
/>

# Provider and styles

Wrap the part of your application that uses Breeze in `BreezeProvider locale="…"`. The provider establishes a shared locale and the root for Breeze styles. Place it above your pages and features so components share this setup.

`styles.css` publishes fonts, private tokens, scoped normalization and compiled utilities. `reset.css` adds optional normalization inside the same root. Neither paints unrelated application content, even if it uses matching utility class names.

Colour-scheme selection applies at the document level, while visual styles are scoped to the Breeze root. Your application remains responsible for the surrounding page layout and background.

With no override, CSS follows `prefers-color-scheme` without JavaScript. An explicit document attribute wins:

```html
<html data-theme="dark"></html>
```

Use `light` or `dark`; removing the attribute restores the OS default. The library does not read or write browser storage.

The brand fill stays constant between schemes to preserve white-label contrast. Brand text and the neutral ramp change. Danger fill also stays dark enough for white labels; the separate danger text token becomes lighter on dark surfaces.
