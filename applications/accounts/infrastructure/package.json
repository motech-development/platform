{
  "name": "@accounts/infrastructure",
  "version": "0.0.0",
  "description": "Motech Development accounts infrastructure",
  "private": true,
  "scripts": {
    "deploy": "yarn run -T serverless deploy --verbose",
    "teardown": "yarn run -T serverless remove --verbose"
  }
}
