type Clients {
  id: ID!
  items: [Client!]!
  nextToken: String
}

type Client {
  address: Address!
  companyId: String!
  contact: Contact!
  id: ID!
  name: String!
}

input ClientInput {
  address: AddressInput!
  companyId: String!
  contact: ContactInput!
  id: ID!
  name: String!
}
