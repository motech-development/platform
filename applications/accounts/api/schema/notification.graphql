type Notification @aws_iam @aws_oidc {
  createdAt: String
  id: ID
  message: String
  read: Boolean
}

type Notifications @aws_iam @aws_oidc {
  id: ID
  items: [Notification]
}

input NotificationInput {
  createdAt: String!
  id: ID!
  message: String!
  read: Boolean!
}

input MarkNotificationsInput {
  ids: [ID]!
}
