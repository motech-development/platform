type Notification {
  createdAt: String
  id: ID
  message: String
  read: Boolean
}

type Notifications {
  id: ID
  items: [Notification]
}

input NotificationInput {
  createdAt: String!
  id: ID!
  message: String!
  read: Boolean!
}

input MarkNotificationsInput {
  ids: [ID]!
}
