{
  "name": "@accounts/warm-up",
  "version": "0.0.0",
  "description": "Motech Development accounts warm up service",
  "private": true,
  "scripts": {
    "deploy": "yarn run -T serverless deploy --verbose",
    "lint": "yarn run -T eslint -c .eslintrc.yml './src/**/*.ts'",
    "precommit": "yarn run -T lint-staged",
    "prettier": "yarn run -T prettier --config ../../../.prettierrc.yml --write 'src/**/*'",
    "start": "yarn run -T serverless invoke stepf --name warmUp",
    "teardown": "yarn run -T serverless remove --verbose",
    "test": "yarn run -T jest --watch",
    "test:ci": "yarn run -T jest --coverage --runInBand"
  },
  "jest": {
    "collectCoverageFrom": [
      "src/**/*.{ts,tsx}",
      "!src/**/*.d.ts"
    ],
    "setupFiles": [
      "jest-date-mock"
    ],
    "testEnvironment": "node"
  },
  "dependencies": {
    "@motech-development/logger": "^0.0.0",
    "luxon": "^2.0.2",
    "uuid": "^8.3.2",
    "yup": "^0.29.1"
  }
}
