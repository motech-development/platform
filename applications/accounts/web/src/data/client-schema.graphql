directive @client on FIELD

extend type Clients {
  clientLoadedPageCount: Int!
  clientRequestedPageCount: Int!
  clientRefreshGeneration: Int!
}

extend type Transactions {
  transactionLoadedPageCount: Int!
  transactionRequestedPageCount: Int!
  transactionRefreshGeneration: Int!
}
