{
  "name": "@motech-development/id",
  "version": "0.0.0",
  "description": "Motech Development universal login",
  "private": true,
  "scripts": {
    "deploy": "a0deploy import -c config.json -i ./src/tenant.yaml",
    "import": "a0deploy export -c config.local.json -f yaml -o ./src"
  },
  "devDependencies": {
    "auth0-deploy-cli": "^3.6.6"
  }
}
