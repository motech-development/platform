{
  "name": "@motech-development/id",
  "version": "0.0.0",
  "description": "Motech Development authentication application",
  "private": true,
  "scripts": {
    "deploy": "a0deploy import -c config.json -i tenant.yaml",
    "prettier": "prettier --config ../../.prettierrc.yml --check 'src/**/*'"
  },
  "devDependencies": {
    "auth0-deploy-cli": "^3.6.6"
  }
}
