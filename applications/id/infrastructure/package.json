{
  "name": "@id/infrastructure",
  "version": "0.0.0",
  "description": "Motech Development infrastructure as code",
  "private": true,
  "scripts": {
    "deploy": "serverless deploy --verbose --force",
    "teardown": "serverless remove --verbose"
  }
}
