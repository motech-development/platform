describe('balance', () => {
  describe('with no table set', () => {
    it.todo('should throw an error');
  });

  describe('with table set', () => {
    it.todo('should do nothing if there is nothing to process');

    it.todo('should begin transaction if there are things to process');
  });
});
