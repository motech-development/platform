{
  "name": "@cypress/storage",
  "version": "0.0.0",
  "description": "Cypress testing storage",
  "private": true,
  "scripts": {
    "deploy": "serverless deploy --verbose --force",
    "teardown": "serverless remove --verbose"
  }
}
