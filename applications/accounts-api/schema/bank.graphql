type Bank {
  id: ID
  name: String
}

type Banks {
  items: [Bank]
}

type BankConnect {
  status: String
  authorisationUrl: String
}

input BankConnectInput {
  bank: String!
  callback: String!
  user: String
}

input BankCallbackInput {
  id: ID!
  authorisationUrl: String!
}
