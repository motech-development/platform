type BankAccountIdentification {
  identification: String
  type: String
}

type BankAccount {
  accountIdentifications: [BankAccountIdentification]
  balance: Float
  currency: String
  id: ID
  type: String
}

type BankAccounts {
  items: [BankAccount]
}
